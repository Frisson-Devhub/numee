import { PORTAL_SESSION_COOKIES } from "@numee/shared/server";

/** Candidate portal session cookie name (aligned with Nest `setPortalSessionCookie`). */
export const CANDIDATE_SESSION_COOKIE = PORTAL_SESSION_COOKIES.candidate;
