import { PORTAL_SESSION_COOKIES } from "@numee/shared/server";

/** Recruiter portal session cookie name (aligned with Nest `setPortalSessionCookie`). */
export const RECRUITER_SESSION_COOKIE = PORTAL_SESSION_COOKIES.recruiter;
